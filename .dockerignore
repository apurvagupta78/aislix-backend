.venv/
__pycache__/
*.pyc
.env
.idea/
product_*.jpg
aislix_crops_*/
audit_report.pdf
*.log
test_scan_output.json
build_faiss.log
# Keep models/*.pt in the Docker context when Railway buildCommand runs `git lfs pull`.
# .git/ is not available inside Docker; LFS must be materialized before `docker build`.
