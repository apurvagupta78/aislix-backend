# Optional PaddleOCR stack — install locally only; not used on Railway (numpy conflicts).
# pip install paddlepaddle==3.2.0 -i https://www.paddlepaddle.org.cn/packages/stable/cpu/
# pip install paddleocr==2.9.1

paddleocr==2.9.1
